// import { TwoWaterJugRiddle, JugSolution } from "./TwoWaterJugRiddle";
// import { sampleSolver } from "./WaterJugSolver";

const root: HTMLElement = document.getElementById("root")!;
root.textContent = "hi root";

// const riddle = new TwoWaterJugRiddle(sampleSolver);
// const solution: JugSolution = riddle.solveRiddle(4, 3, 2);

// const list = document.getElementById("outputList")!;
// if (solution.length > 0) {
//   solution.forEach((s) => {
//     const li = document.createElement("li");
//     list.appendChild(document.createTextNode(s.toString()));
//   });
// } else {
//   const li = document.createElement("li");
//   list.appendChild(document.createTextNode("No Solution"));
// }

// export {};