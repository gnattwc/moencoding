import {JugState, JugSolution} from "./TwoWaterJugRiddle";

function sampleSolver(jug1:number, jug2:number, aim:number, initState: JugState): JugSolution {
  // return [{ action: JugAction.Init, state: [0, 0] }];
  return [];
}

export { sampleSolver };
